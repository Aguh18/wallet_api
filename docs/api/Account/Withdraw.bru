meta {
  name: "Withdraw"
  type: http
  seq: 11
}

post {
  url: {{base_url}}/v1/accounts/:id/withdraw
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Cookie: access_token={{access_token}}; refresh_token={{refresh_token}}
}

params:path {
  id: {{account_id}}
}

body:json {
  {
    "amount": 50000,
    "description": "ATM Withdrawal"
  }
}
