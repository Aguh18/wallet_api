meta {
  name: "Get Wallet"
  type: http
  seq: 9
}

get {
  url: {{base_url}}/v1/wallets/:id
  auth: none
}

headers {
  Cookie: access_token={{access_token}}; refresh_token={{refresh_token}}
}

params:path {
  id: {{wallet_id}}
}
