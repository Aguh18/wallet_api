meta {
  name: "Get Transactions"
  type: http
  seq: 13
}

get {
  url: {{base_url}}/v1/wallets/:id/transactions
  auth: none
}

headers {
  Cookie: access_token={{access_token}}; refresh_token={{refresh_token}}
}

params:path {
  id: {{wallet_id}}
}

query {
  limit: 10
  offset: 0
}
