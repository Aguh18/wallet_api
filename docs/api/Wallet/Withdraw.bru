meta {
  name: "Withdraw"
  type: http
  seq: 11
}

post {
  url: {{base_url}}/v1/wallets/:id/withdraw
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Cookie: access_token={{access_token}}; refresh_token={{refresh_token}}
}

params:path {
  id: {{wallet_id}}
}

body:json {
  {
    "amount": "50000.25",
    "description": "ATM Withdrawal"
  }
}
