meta {
  name: "Get Account"
  type: http
  seq: 9
}

get {
  url: {{base_url}}/v1/accounts/:id
  body: none
  auth: none
}

headers {
  Cookie: access_token={{access_token}}; refresh_token={{refresh_token}}
}

params:path {
  id: {{account_id}}
}
