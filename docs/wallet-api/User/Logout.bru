meta {
  name: "Logout"
  type: http
  seq: 5
}

post {
  url: {{base_url}}/v1/auth/logout
  body: none
  auth: none
}

headers {
  Cookie: access_token={{access_token}}; refresh_token={{refresh_token}}
}
